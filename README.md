# Minecraft Server Installer 🎮

[![License](https://img.shields.io/badge/license-MIT-blue.svg)](LICENSE)
[![Platform](https://img.shields.io/badge/platform-Linux-lightgrey.svg)](https://www.linux.org/)
[![Minecraft](https://img.shields.io/badge/Minecraft-Server-orange.svg)](https://www.minecraft.net/)

Автоматический установщик Minecraft сервера для VDS/Linux систем с поддержкой различных ядер и версий.

## 📋 Описание

Этот скрипт автоматизирует установку Minecraft сервера на Linux VDS. Он поддерживает установку различных версий Minecraft и ядер сервера (Vanilla, Paper, Spigot, Fabric, Forge), настраивает необходимые параметры и создает удобные скрипты для управления сервером.

## ✨ Возможности

- ✅ Автоматическая установка Java 17 (OpenJDK)
- ✅ Выбор версии Minecraft из списка или ручной ввод
- ✅ Поддержка различных ядер сервера:
  - **Vanilla** - официальный сервер от Mojang
  - **Paper** - оптимизированный сервер (рекомендуется)
  - **Spigot** - оптимизированный сервер с поддержкой плагинов
  - **Fabric** - для модов
  - **Forge** - для модов
- ✅ Автоматическое скачивание серверных файлов через официальные API
- ✅ Настройка конфигурации сервера (server.properties)
- ✅ Создание скриптов управления:
  - Запуск/остановка сервера
  - Подключение к консоли
  - Просмотр логов
  - Отправка команд
- ✅ Опциональная настройка systemd service для автозапуска
- ✅ Запуск сервера в screen сессии для работы в фоне

## 🔧 Требования

- Linux система (Debian/Ubuntu/CentOS/RHEL/Fedora)
- Права root (sudo)
- Интернет-соединение для скачивания файлов
- Минимум 2GB RAM (рекомендуется больше)

## 🚀 Быстрый старт

### Установка

1. Скачайте скрипт:
```bash
wget https://raw.githubusercontent.com/ваш-username/репозиторий/main/install-minecraft.sh
```

Или клонируйте репозиторий:
```bash
git clone https://github.com/ваш-username/репозиторий.git
cd репозиторий
```

2. Сделайте скрипт исполняемым:
```bash
chmod +x install-minecraft.sh
```

3. Запустите установку:
```bash
sudo ./install-minecraft.sh
```

4. Следуйте инструкциям установщика:
   - Выберите путь для установки (по умолчанию: `/opt/minecraft`)
   - Выберите версию Minecraft (1.20.4, 1.20.1, 1.19.4 и т.д.)
   - Выберите ядро сервера (Vanilla, Paper, Spigot, Fabric, Forge)
   - Настройте параметры сервера (порт, память, количество игроков и т.д.)
   - Выберите, создавать ли systemd service

### Пример установки

```bash
# Скачать скрипт
wget https://raw.githubusercontent.com/ваш-username/репозиторий/main/install-minecraft.sh

# Установить права
chmod +x install-minecraft.sh

# Запустить установку
sudo ./install-minecraft.sh

# Выбрать в установщике:
# - Путь: /opt/minecraft (Enter)
# - Версия: 1 (1.20.4)
# - Ядро: 2 (Paper)
# - Порт: 25565 (Enter)
# - Память: 4 (4GB)
# - Systemd service: y (да)
```

## 📖 Использование

### Управление сервером

После установки в директории сервера будут созданы следующие скрипты:

#### Запуск сервера
```bash
cd /opt/minecraft  # или ваш путь установки
./start.sh
```

Сервер запустится в screen сессии с именем `minecraft`.

#### Остановка сервера
```bash
./stop.sh
```

#### Подключение к консоли сервера
```bash
./console.sh
```

После подключения вы сможете вводить команды напрямую в консоль Minecraft.

**Важно:** Для отключения от консоли без остановки сервера нажмите `Ctrl+A`, затем `D`.

#### Просмотр логов
```bash
./logs.sh
```

Показывает последние 50 строк логов. Можно включить просмотр в реальном времени (tail -f).

#### Отправка команды без подключения
```bash
./command.sh "say Привет всем!"
./command.sh "list"
./command.sh "give PlayerName diamond 64"
./command.sh "op PlayerName"
```

Полезно для автоматизации и скриптов.

### Управление через systemd (если был создан service)

```bash
# Запуск
sudo systemctl start minecraft

# Остановка
sudo systemctl stop minecraft

# Статус
sudo systemctl status minecraft

# Просмотр логов
sudo journalctl -u minecraft -f

# Автозапуск при загрузке системы
sudo systemctl enable minecraft
```

## 🎮 Полезные команды Minecraft

После подключения к консоли (`./console.sh`) или через `./command.sh` можно использовать:

| Команда | Описание |
|---------|----------|
| `list` | Показать список игроков онлайн |
| `op <ник>` | Выдать права оператора игроку |
| `deop <ник>` | Убрать права оператора |
| `whitelist add <ник>` | Добавить игрока в белый список |
| `whitelist remove <ник>` | Удалить из белого списка |
| `ban <ник>` | Забанить игрока |
| `pardon <ник>` | Разбанить игрока |
| `say <сообщение>` | Отправить сообщение всем игрокам |
| `give <ник> <предмет> [количество]` | Выдать предмет игроку |
| `gamemode <режим> [ник]` | Изменить режим игры (survival, creative, adventure, spectator) |
| `tp <ник1> <ник2>` | Телепортировать игрока |
| `save-all` | Сохранить мир |
| `stop` | Остановить сервер |

## 🔥 Настройка файрвола

Обязательно откройте порт сервера в файрволе:

### Ubuntu/Debian (ufw)
```bash
sudo ufw allow 25565/tcp
sudo ufw reload
```

### CentOS/RHEL/Fedora (firewalld)
```bash
sudo firewall-cmd --permanent --add-port=25565/tcp
sudo firewall-cmd --reload
```

### iptables
```bash
sudo iptables -A INPUT -p tcp --dport 25565 -j ACCEPT
sudo iptables-save
```

## 📁 Структура файлов

После установки в директории сервера будут созданы:

```
/opt/minecraft/
├── server.jar           # Серверный файл
├── eula.txt             # Лицензионное соглашение
├── server.properties    # Конфигурация сервера
├── start.sh             # Скрипт запуска
├── stop.sh              # Скрипт остановки
├── console.sh           # Скрипт подключения к консоли
├── logs.sh              # Скрипт просмотра логов
├── command.sh           # Скрипт отправки команд
├── logs/                # Папка с логами
│   └── latest.log       # Последний лог
├── world/               # Папка с миром (создается при первом запуске)
└── ...
```

## ⚙️ Настройка параметров

Основные параметры сервера настраиваются в файле `server.properties`:

- `server-port` - порт сервера (по умолчанию: 25565)
- `max-players` - максимальное количество игроков
- `online-mode` - проверка лицензий (true/false)
- `white-list` - белый список (true/false)
- `difficulty` - сложность (peaceful, easy, normal, hard)
- `gamemode` - режим игры по умолчанию
- `spawn-monsters` - спавн мобов (true/false)
- `pvp` - PvP включен (true/false)

После изменения `server.properties` перезапустите сервер.

## 🐛 Решение проблем

### Сервер не запускается

1. Проверьте логи:
```bash
./logs.sh
```

2. Проверьте, что Java установлена:
```bash
java -version
```

3. Проверьте, что порт свободен:
```bash
sudo netstat -tulpn | grep 25565
```

4. Проверьте права доступа:
```bash
ls -la
```

### Не могу подключиться к консоли

1. Проверьте, что сервер запущен:
```bash
screen -list
```

2. Если сервер не в списке, перезапустите его:
```bash
./start.sh
```

### Проблемы с памятью

Если сервер крашится из-за нехватки памяти, измените размер памяти в `start.sh`:
```bash
nano start.sh
# Измените -Xms2G -Xmx2G на нужные значения (например, -Xms4G -Xmx4G)
```

### Не могу скачать серверные файлы

Проверьте интернет-соединение и доступность API:
```bash
curl https://api.papermc.io/v2/projects/paper
curl https://piston-meta.mojang.com/mc/game/version_manifest_v2.json
```

## 🔒 Безопасность

- ✅ Запускайте сервер от отдельного пользователя (не от root)
- ✅ Настройте белый список, если сервер приватный
- ✅ Регулярно обновляйте сервер на последние версии
- ✅ Делайте резервные копии мира (`world/` папки)
- ✅ Настройте правильные права доступа к файлам сервера

### Создание пользователя для сервера

```bash
sudo useradd -r -s /bin/false minecraft
sudo chown -R minecraft:minecraft /opt/minecraft
```

## 💾 Резервное копирование

Рекомендуется регулярно делать резервные копии:

```bash
# Создать резервную копию мира
tar -czf backup-$(date +%Y%m%d-%H%M%S).tar.gz world/

# Восстановить из резервной копии
tar -xzf backup-YYYYMMDD-HHMMSS.tar.gz
```

Можно настроить cron для автоматического бэкапа:
```bash
# Добавить в crontab (crontab -e)
0 2 * * * cd /opt/minecraft && tar -czf /backups/minecraft-$(date +\%Y\%m\%d).tar.gz world/
```

## 📝 Лицензия

Этот скрипт распространяется "как есть" без каких-либо гарантий. Minecraft является товарным знаком Mojang Studios.

## 🤝 Вклад

Приветствуются pull requests и issues! Если нашли ошибку или хотите добавить функцию, создайте issue или pull request.

## 📞 Поддержка

Если у вас возникли проблемы:
1. Проверьте раздел "Решение проблем"
2. Посмотрите логи: `./logs.sh`
3. Создайте issue в репозитории

## 🙏 Благодарности

- [Mojang Studios](https://www.minecraft.net/) - за Minecraft
- [PaperMC](https://papermc.io/) - за оптимизированное ядро
- [SpigotMC](https://www.spigotmc.org/) - за Spigot
- [Fabric](https://fabricmc.net/) - за Fabric loader
- [Minecraft Forge](https://files.minecraftforge.net/) - за Forge

---

⭐ Если этот скрипт был полезен, поставьте звезду!

